# TMDB key, used server-side only by /api/tmdb. Either kind works:
# - v3 "API Key" (short alphanumeric string) — sent as ?api_key=
# - v4 "Read Access Token" (long eyJ... JWT) — sent as Authorization: Bearer
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
